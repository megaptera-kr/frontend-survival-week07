import { render } from '@testing-library/react';
import FilterableRestaurantTable from './FilterableRestaurantTable';

describe('FilterRestaurantTable', () => {
  it('render FilterRestaurantTable', () => {
    render(<FilterableRestaurantTable />);
  });
});
